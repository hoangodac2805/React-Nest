export const API_RESPONSE_MESSAGE = {
    INVALID_REFRESH_TOKEN: ["Invalid or expired refresh token"],
    INVALID_CREDENTIALS: ["Invalid credentials"],
    INVALID_ACCESS_TOKEN: ["jwt expired", "Invalid access token"]
}